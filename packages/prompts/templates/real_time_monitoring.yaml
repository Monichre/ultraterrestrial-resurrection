id: real_time_monitoring
version: "0.1.0"
description: "Monitoring prompt skeleton (to be refined)."
owner: "ai-platform"
tags: ["monitoring"]
schema_ref: null
runtime:
  max_tokens: 900
  temperature: 0.2
variables:
  - name: "signal"
    type: "string"
    required: true
prompt: |-
  You are monitoring a real-time signal: {{signal}}
  Define thresholds, anomaly criteria, and alerting rules.
