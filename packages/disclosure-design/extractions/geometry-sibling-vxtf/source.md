---
title: Source — geometry-sibling-vxtf
description: GOLD tier — vision-written 2026-08-13 (self).
type: note
created: 2026-08-13
author: agent
tags: [folderize, source, gold, vision-self]
---

# Source — `geometry-sibling-vxtf`

Provenance: **self** (vision-read of `geometry-sibling-vxtf.png`, 928×1232, this session). Cluster rep for `mj:…vXtf5VcgeXU…` ("Geometry sibling").

## Aesthetics analysis (per `prompts/aesthetics.md`)

**Aspects/Subjects/Motifs:** a sparse vertical stack of geometric line constructions on a near-pure-black field — a wireframe pyramid with internal construction lines near top, a faint orbital ring system mid-frame, small tick-marks and dimension lines scattered between; the marks are dim, as if drawn in grey chalk on black.

**Adjectives:** austere, nocturnal, schematic, withheld, minimal, chalk-on-black, quiet.

**Display/Medium/Usage-Context:** a dark schematic plate — geometry as notation rather than illustration; the *night register* of the line-geometry language, sibling to the brighter construction plates.

**Genres/Styles:** black-board geometry; dark-mode schematic; the far dark end of the line-geometry cluster — where `line-geometry-bwpx` reads as a lit construction, this is the same vocabulary at near-darkness.

**Color Palette:** near-total black `#000000` (79.4% share, pixel ✅) — one of the darkest frames in the vault — with a faint ladder of greys `#191a1a`, `#343433`, `#565656` (2.5%, 2.86:1 vs bg) carrying the line work. Median = dominant = `#000000`. Achromatic; no warmth.

**Composition:** portrait; constructions arranged in a loose vertical stack with generous black between; no grid lines, no border — the marks float; the eye travels top to bottom through diminishing light.

**Emotional Impact:** hushed concentration — geometry whispered rather than stated; the feeling of a chalkboard in a dark room.

**Other Details:** distinct within the geometry cluster as the *dim* sibling — same constructions as the lit plates, but at a value range (max 2.86:1) that forces the viewer to lean in. Pairs with `line-geometry-bwpx` as dark/light twins.

## Dense description

```
sparse vertical stack of geometric line constructions on near-pure-black field, wireframe pyramid with internal construction lines, faint orbital ring system, small tick-marks and dimension lines, dim grey chalk on black, austere nocturnal schematic withheld minimal, near-total black #000000 79 percent of frame faint grey ladder #191a1a #343433 #565656, portrait loose vertical stack generous black between marks float, blackboard geometry dark-mode schematic, the dim sibling of the line-geometry cluster, same vocabulary at near-darkness, hushed concentration geometry whispered, chalkboard in a dark room, max contrast only 2.86:1 forcing the viewer to lean in
```
