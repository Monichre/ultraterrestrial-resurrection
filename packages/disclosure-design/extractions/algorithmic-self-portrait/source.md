---
title: Source — algorithmic-self-portrait
description: GOLD tier — vision-written 2026-08-13 (self).
type: note
created: 2026-08-13
author: agent
tags: [folderize, source, gold, vision-self]
---

# Source — `algorithmic-self-portrait`

Provenance: **self** (vision-read of `algorithmic-self-portrait.png`, 1400×1800, this session).

## Aesthetics analysis (per `prompts/aesthetics.md`)

**Aspects/Subjects/Motifs:** a human portrait (the author — "Liam", seed-004) half-dissolved into blackness — face and head barely modeled by faint warm-grey contour and tonal line, emerging from a field that is 85.7% pure near-black.

**Adjectives:** subterranean, minimal, introspective, spectral, withheld, extreme low-key.

**Display/Medium/Usage-Context:** generative/algorithmic portrait study — a "seed" artwork (seed_004 in the filename), identity-adjacent art rather than illustration.

**Genres/Styles:** generative portraiture; tenebrism pushed to near-abstraction; the far dark end of the vault's line-on-dark language.

**Color Palette:** near-total black `#090909` (85.7% share, pixel ✅) with a single faint warm-grey tone `#2b2927` (3.1%) carrying the entire figure; median = dominant = `#090909` — the darkest measured frame in the vault.

**Composition:** portrait field; the face occupies the middle darkness with no hard edges — form is implied by the faintest tonal deviation, so the eye has to work.

**Emotional Impact:** presence barely surfacing from void — introspective, anonymous, a little haunting; identity as something the algorithm can only half-recover.

**Other Details:** this is the extreme end of the dark register — where `prometheus-geometry-mix` is luminous-on-black, this is *barely-there-on-black*. Useful as the floor of the vault's tonal ladder; any darker and the image is gone.

## Dense description

```
algorithmic self portrait seed-004, human face half-dissolved into near-total blackness, faintest warm-grey contour and tonal line barely modeling the head, 85 percent pure near-black field, extreme low-key tenebrism pushed to near-abstraction, generative portraiture, subterranean spectral withheld introspective, presence barely surfacing from void, identity half-recovered by the algorithm, darkest frame in the vault, portrait composition form implied by faint tonal deviation, the eye has to work
```
