# Visual lab binaries — never git-add the 2.6G vault
_corpus/
extractions/**/*.png
extractions/**/*.jpg
extractions/**/*.jpeg
extractions/**/*.webp
extractions/**/*.gif
design/Archive.zip
workers/
.git/

# Large stills at package root only (design/ mock-ups and design-lab art may be tracked)
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif

# OS / editor noise
.DS_Store
# macOS custom-folder-icon file is literally "Icon\r" — bare `Icon` never matches it.
Icon?
Icon\r
.specstory/
.impeccable/
