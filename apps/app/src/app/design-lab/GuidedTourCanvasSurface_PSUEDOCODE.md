# GuidedTourCanvasSurface — Variant F synthesis

## Inputs

- Like A (graph-primary HUD) and D (path = tour UI)
- Dislike: intelligence as card / sidebar / drawer
- Want: intel baked into record OR visual connections / waypoints
- Side menu: hide/show when unused
- Reference: apossible.com (constellation canvas, chrome-minimal, logo-as-menu)

## Variant F algorithm

1. Floor = CanvasChrome with:
   - `navMode=auto-hide` → peek tab + left hotzone; pin optional
   - `showEdgeLabels` → mid-edge mono labels (solid / dashed affinity / tour path)
   - `bakeIntelIntoRecord` → active node grows evidentiary stamps + hypothesis + affinity
2. Overlay A-style slim top HUD (tour title, step, year, stop) — no intel stamp
3. Overlay D-style path narrator on the amber tour edge (Back / Continue only)
4. No right rail, no Tour intelligence card, no floating AI affinity chip

## apossible → UT mapping

- Chrome-minimal nav → auto-hide side rail (not cream reading panel — keep Microfilm Dark)
- Constellation edges carry meaning → edge labels = intelligence layer
- Persistent map during detail → graph never replaced by intel panel
- Taxonomy glyphs → evidentiary stamps on the record itself
