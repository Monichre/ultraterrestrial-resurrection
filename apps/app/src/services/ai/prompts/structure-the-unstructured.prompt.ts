export const structureTheUnstructuredPrompt = `

Now take the content you've structured and convert it into a well-organized table format using JSON as the next step in the processing procedure prior to insertion in the database. Identify the main entities, attributes, or categories mentioned in the text and use them as keys in the JSON object. Then, extract the relevant information from the text and populate the corresponding values in the JSON object. Ensure that the data is accurately represented and properly formatted within the JSON structure. The resulting JSON table should provide a clear, structured overview of the information presented in the original text as per your previous instructions.
`
