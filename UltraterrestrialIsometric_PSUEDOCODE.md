# Ultraterrestrial isometric — pseudocode

## Height

`h = min(7, max(1, 1 + round(6 * (log10(max(loc,50)) - 1.70) / 3.37)))`
Clamp because `apps/app/src/components` (116,474) would dwarf the city on a linear scale.

## Lenses

1. ws — apps/app vs packages vs Python RAG vs lab vs storage
2. flow — Research Canvas → mindmap API → OpenAI / Neon / Exa → SSE graph writes
3. live — Vercel Next vs disconnected disclosure-rag vs Clerk-not-on-canvas
4. seq — eight junctures from `Index()` through `searchDatabase` / Exa back to `splitSseLines`

## Layout

Tallest (`ui`) at (0,0). RAG island at gx=8. Storage slabs on gy=6.

## Verify

Playwright 1700×950 via localhost; every lens; seq arrows + bar; Talks-to chips.
